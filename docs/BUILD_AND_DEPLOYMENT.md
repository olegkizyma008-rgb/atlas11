# Build and Deployment Guide

**Дата:** 10 грудня 2025, 23:59 UTC+02:00  
**Статус:** ✅ **FULLY DOCUMENTED**

---

## 🔨 Що Створюється При Білді?

### 1. **TypeScript Компіляція** (npm run build)

```bash
npm run build
```

**Що створюється:**

```
out/
├── main/                    ← Скомпільований Electron main process
│   ├── index.js
│   ├── router.js
│   └── ...
├── renderer/                ← Скомпільований React UI
│   ├── index.html
│   ├── assets/
│   │   ├── index-*.css
│   │   └── index-*.js
│   └── ...
└── kontur/                  ← Скопійовані Python органи
    ├── organs/
    │   ├── tetyana_agent.py
    │   ├── tetyana_bridge.py
    │   ├── rag_indexer.py
    │   └── ...
    └── ag/
        └── ...
```

**Статус:** ✅ Компіляція TypeScript + копіювання Python файлів

### 2. **Бінарник (bin/tetyana)** ❌ НЕ ПЕРЕБУДОВУЄТЬСЯ

```bash
bin/tetyana
```

**Що це:**
- Bash скрипт (не скомпільований)
- Вже існує в репозиторії
- Не потребує перебудови

**Статус:** ✅ Статичний файл, не змінюється

---

## 📊 Архітектура Білду

```
npm run build
    ↓
electron-vite build
    ├─ Компіляція src/main/ → out/main/
    ├─ Компіляція src/renderer/ → out/renderer/
    └─ Компіляція src/kontur/ → out/kontur/
    ↓
copy-assets (npm скрипт)
    ├─ mkdir -p out/kontur/organs
    ├─ mkdir -p out/kontur/ag
    ├─ cp src/kontur/organs/*.py → out/kontur/organs/
    └─ cp src/kontur/ag/*.py → out/kontur/ag/
    ↓
✅ Build завершено
```

---

## 🎯 Що Саме Створюється?

### 1. **Electron App** (Для GUI)

```
out/main/                   ← Electron main process (TypeScript → JavaScript)
out/renderer/               ← React UI (TypeScript → JavaScript + HTML + CSS)
```

**Для:** Графічний інтерфейс додатка  
**Мова:** TypeScript → JavaScript  
**Потребує перебудови:** ✅ ДА (при змінах в src/main/ або src/renderer/)

### 2. **KONTUR System** (Для Логіки)

```
out/kontur/                 ← Скопійовані Python органи та AG система
├── organs/                 ← Python органи
│   ├── tetyana_agent.py
│   ├── tetyana_bridge.py
│   ├── rag_indexer.py
│   └── ...
└── ag/                     ← AG система
    └── ...
```

**Для:** Основна логіка системи  
**Мова:** Python  
**Потребує перебудови:** ❌ НІ (копіюється як є)

### 3. **Binary Wrapper** (Для Запуску)

```
bin/tetyana                 ← Bash скрипт (статичний)
```

**Для:** Запуск Python агента  
**Мова:** Bash  
**Потребує перебудови:** ❌ НІ (статичний файл)

---

## 🔄 Цикл Розробки

### Сценарій 1: Змінити TypeScript (UI/Main)

```bash
# Змінити src/main/index.ts або src/renderer/App.tsx
vim src/main/index.ts

# Перебудувати
npm run build

# Результат: out/main/ та out/renderer/ оновлені
```

**Потребує перебудови:** ✅ ДА

### Сценарій 2: Змінити Python (Логіка)

```bash
# Змінити src/kontur/organs/tetyana_agent.py
vim src/kontur/organs/tetyana_agent.py

# Перебудувати (для копіювання)
npm run build

# Або просто запустити (Python читається з src/)
./bin/tetyana "завдання"
```

**Потребує перебудови:** ⚠️ ЗАЛЕЖИТЬ
- Для запуску через `./bin/tetyana` — ❌ НІ (читає з src/)
- Для запуску через `npm run cli` — ✅ ДА (використовує out/)

### Сценарій 3: Змінити Конфіг (.env)

```bash
# Змінити .env
vim .env

# Перебудувати: ❌ НІ (конфіг не компілюється)
# Просто перезапустити агент
./bin/tetyana "завдання"
```

**Потребує перебудови:** ❌ НІ

### Сценарій 4: Змінити Бінарник (bin/tetyana)

```bash
# Змінити bin/tetyana
vim bin/tetyana

# Перебудувати: ❌ НІ (bash скрипт, не компілюється)
# Просто запустити
./bin/tetyana "завдання"
```

**Потребує перебудови:** ❌ НІ

---

## 📋 Build Commands

### Основна Компіляція

```bash
npm run build
```

**Що робить:**
1. Компілює TypeScript (src/ → out/)
2. Копіює Python органи (src/kontur/ → out/kontur/)
3. Готує Electron app

**Час:** ~1-2 секунди

### Development Mode

```bash
npm run dev
```

**Що робить:**
1. Запускає electron-vite у watch mode
2. Автоматично перебудовує при змінах
3. Hot reload для UI

**Час:** Постійно слухає

### CLI Mode

```bash
npm run cli
```

**Що робить:**
1. Запускає інтерактивне меню
2. Використовує скомпільовані файли з out/
3. Для розробки можна використовувати src/ напряму

---

## 🔧 Що Потребує Перебудови?

| Файл/Папка | Змінюється | Потребує Build |
|-----------|-----------|----------------|
| src/main/ | TypeScript | ✅ ДА |
| src/renderer/ | TypeScript | ✅ ДА |
| src/kontur/organs/ | Python | ⚠️ ЗАЛЕЖИТЬ |
| src/kontur/ag/ | Python | ⚠️ ЗАЛЕЖИТЬ |
| src/cli/ | TypeScript | ✅ ДА |
| bin/tetyana | Bash | ❌ НІ |
| .env | Конфіг | ❌ НІ |
| docs/ | Документація | ❌ НІ |
| rag/ | RAG база | ❌ НІ |

---

## 🚀 Deployment

### Для Development

```bash
# 1. Встановити залежності
npm install
pip install -r requirements.txt

# 2. Перебудувати
npm run build

# 3. Запустити
./bin/tetyana "завдання"
# або
npm run cli
```

### Для Production

```bash
# 1. Встановити залежності
npm ci  # Точні версії з package-lock.json
pip install -r requirements.txt

# 2. Перебудувати
npm run build

# 3. Запустити Electron app
npm start
# або запустити агента
./bin/tetyana "завдання"
```

### Для Docker (Опціонально)

```dockerfile
FROM node:18-alpine

WORKDIR /app

# Встановити залежності
COPY package*.json ./
RUN npm ci

# Встановити Python залежності
COPY requirements.txt ./
RUN pip install -r requirements.txt

# Копіювати код
COPY . .

# Перебудувати
RUN npm run build

# Запустити
CMD ["./bin/tetyana"]
```

---

## 🎯 Рекомендована Робоча Послідовність

### Для Розробки

```bash
# 1. Запустити dev mode (watch)
npm run dev

# 2. У іншому терміналі запустити агента
./bin/tetyana "завдання"

# 3. Редагувати файли
vim src/main/index.ts
vim src/kontur/organs/tetyana_agent.py

# 4. Зміни автоматично перебудовуються (для TypeScript)
# 5. Python змінюються відразу (без перебудови)
```

### Для Production

```bash
# 1. Перебудувати все
npm run build

# 2. Запустити
./bin/tetyana "завдання"
# або
npm start  # Electron app
```

---

## 📊 Порівняння: Що Потребує Перебудови

| Компонент | Тип | Перебудова | Час |
|-----------|-----|-----------|-----|
| Electron Main | TypeScript | ✅ ДА | 1-2 сек |
| React UI | TypeScript | ✅ ДА | 1-2 сек |
| Python Органи | Python | ❌ НІ | - |
| Bash Бінарник | Bash | ❌ НІ | - |
| Конфіг | YAML/JSON | ❌ НІ | - |
| Документація | Markdown | ❌ НІ | - |

---

## 🔒 Безпека

### ✅ Безпечно

- Компіляція видаляє вихідний код TypeScript
- Python органи залишаються в проекті
- Бінарник не має прав на запис в систему
- Все в межах проекту

### ⚠️ Важливо

- Не комітити `out/` папку (генерується)
- Не комітити `node_modules/` (генерується)
- Комітити `package-lock.json` (для точних версій)
- Комітити `requirements.txt` (для Python залежностей)

---

## 🎯 Висновок

### ✅ Що Створюється

1. **Electron App** (TypeScript → JavaScript)
   - Потребує перебудови при змінах в src/main/ або src/renderer/
   - Час: 1-2 секунди

2. **Python Органи** (копіюються)
   - НЕ потребують перебудови
   - Читаються з src/ або out/ залежно від запуску

3. **Bash Бінарник** (статичний)
   - НЕ потребує перебудови
   - Просто запускається

### ✅ Рекомендація

- Запускати `npm run dev` для розробки (auto-rebuild)
- Запускати `npm run build` перед production
- Python можна змінювати без перебудови
- Бінарник статичний, не змінюється

---

**Build система оптимізована для швидкої розробки! 🚀**

# Binary Wrapper Architecture

**Дата:** 10 грудня 2025, 23:57 UTC+02:00  
**Статус:** ✅ **FULLY DOCUMENTED**

---

## 📍 Де Знаходиться Бінарник?

### ✅ Локація

```
/Users/dev/Documents/GitHub/atlas/bin/tetyana
```

**Тип:** Bash скрипт (не скомпільований бінарник)  
**Розмір:** 2.003 KB  
**Права:** 755 (rwxr-xr-x)  
**Власник:** dev:staff

---

## 🔍 Як Це Працює?

### 1. **Bash Wrapper (Оболонка)**

```bash
#!/usr/bin/env bash
```

Це **не** скомпільований бінарник, а **bash скрипт**, який:
- Знаходиться в репозиторії проекту
- Виконується як текстовий файл
- Має права на виконання (755)
- Запускає Python агент

### 2. **Відносні Шляхи (Важливо!)**

```bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
```

**Як це працює:**
1. `${BASH_SOURCE[0]}` — шлях до самого скрипту (`bin/tetyana`)
2. `dirname` — отримує директорію (`bin/`)
3. `cd` + `pwd` — отримує абсолютний шлях
4. `dirname` ще раз — отримує батьківську директорію (корінь проекту)

**Результат:** Скрипт знаходить проект незалежно від того, звідки його запустити!

### 3. **Шляхи до Компонентів**

```bash
PYTHON_VENV="$PROJECT_ROOT/python/venv/bin/python3"
KONTUR_BRIDGE="$PROJECT_ROOT/src/kontur/organs/tetyana_bridge.py"
PYTHON_AGENT="$PROJECT_ROOT/src/kontur/organs/tetyana_agent.py"
ENV_FILE="$PROJECT_ROOT/.env"
```

**Все відносно до проекту, не до системи!**

---

## 🚀 Як Запустити?

### Варіант 1: Абсолютний Шлях (Звідкись)

```bash
/Users/dev/Documents/GitHub/atlas/bin/tetyana "завдання"
```

**Результат:** ✅ Працює (скрипт знаходить проект)

### Варіант 2: Відносний Шлях (З кореня проекту)

```bash
cd /Users/dev/Documents/GitHub/atlas
./bin/tetyana "завдання"
```

**Результат:** ✅ Працює (скрипт знаходить проект)

### Варіант 3: З Будь-якої Директорії

```bash
cd /tmp
/Users/dev/Documents/GitHub/atlas/bin/tetyana "завдання"
```

**Результат:** ✅ Працює (скрипт знаходить проект)

### Варіант 4: Додати в PATH (Опціонально)

```bash
# Додати в ~/.zshrc або ~/.bashrc
export PATH="/Users/dev/Documents/GitHub/atlas/bin:$PATH"

# Тепер можна запустити з будь-якого місця
tetyana "завдання"
```

**Результат:** ✅ Працює (якщо додано в PATH)

---

## 🔐 Права Доступу

### Поточні Права

```
-rwxr-xr-x  1 dev  staff  2003 Dec 10 23:01 bin/tetyana
```

**Розшифровка:**
- `r` (read) — можна читати
- `w` (write) — можна писати (тільки власник)
- `x` (execute) — можна виконувати
- `755` — власник: rwx, група: rx, інші: rx

### Де Виконується?

**Не в системному /usr/bin/, а в:**
- Поточній директорії користувача
- Будь-якій директорії (через абсолютний шлях)
- PATH (якщо додано)

**Важливо:** Права 755 означають, що **будь-хто** може виконати скрипт, але тільки **власник** (dev) може редагувати.

---

## 📊 Архітектура Виконання

```
tetyana (bash скрипт в bin/)
    ↓
Визначає PROJECT_ROOT
    ↓
Перевіряє наявність:
├── python/venv/bin/python3
├── src/kontur/organs/tetyana_bridge.py
└── src/kontur/organs/tetyana_agent.py
    ↓
Завантажує .env змінні
    ↓
Запускає Python агент:
    python3 src/kontur/organs/tetyana_bridge.py "завдання"
    ↓
KONTUR Protocol Response (JSON)
```

---

## 🔧 Як Це Працює Детально

### 1. **Скрипт Запускається**

```bash
./bin/tetyana "Відкрий Safari"
```

### 2. **Визначення Шляхів**

```bash
SCRIPT_DIR = /Users/dev/Documents/GitHub/atlas/bin
PROJECT_ROOT = /Users/dev/Documents/GitHub/atlas
```

### 3. **Перевірка Залежностей**

```bash
✅ /Users/dev/Documents/GitHub/atlas/python/venv/bin/python3 — ІСНУЄ
✅ /Users/dev/Documents/GitHub/atlas/src/kontur/organs/tetyana_bridge.py — ІСНУЄ
✅ /Users/dev/Documents/GitHub/atlas/src/kontur/organs/tetyana_agent.py — ІСНУЄ
```

### 4. **Завантаження Конфіг**

```bash
export PYTHONUNBUFFERED=1
export PYTHONDONTWRITEBYTECODE=1
# + змінні з .env (API ключі, тощо)
```

### 5. **Запуск Python Агента**

```bash
/Users/dev/Documents/GitHub/atlas/python/venv/bin/python3 \
  /Users/dev/Documents/GitHub/atlas/src/kontur/organs/tetyana_bridge.py \
  "Відкрий Safari"
```

### 6. **Результат**

```json
{
  "type": "TASK_RESPONSE",
  "source": "tetyana-agent",
  "destination": "kontur-core",
  "payload": { ... },
  "status": "success"
}
```

---

## 💾 Де Зберігаються Дані?

### Python Venv

```
/Users/dev/Documents/GitHub/atlas/python/venv/
```

**Що там:**
- Python інтерпретатор
- Встановлені пакети (langchain, langgraph, тощо)
- Кеш пакетів

### RAG База

```
/Users/dev/Documents/GitHub/atlas/rag/
├── chroma_mac/                          ← Vector DB
├── macOS-automation-knowledge-base/     ← Knowledge base
└── index_rag.py                         ← Індексатор
```

### Конфіг

```
/Users/dev/Documents/GitHub/atlas/.env
```

**Що там:**
- API ключі
- Конфіг змінні
- Шляхи до ресурсів

---

## 🔒 Безпека

### ✅ Безпечно

- Скрипт **не** має прав на запис в системні директорії
- Виконується з правами користувача (dev)
- Не потребує sudo
- Все в межах проекту

### ⚠️ Важливо

- Не додавати в `/usr/local/bin/` без необхідності
- Не запускати з sudo (не потрібно)
- Зберігати `.env` в безпеці (містить API ключі)

---

## 🚀 Рекомендована Установка

### Для Розробки (Поточна Конфіг)

```bash
# Просто запускати з проекту
./bin/tetyana "завдання"
```

**Переваги:**
- ✅ Простота
- ✅ Безпека
- ✅ Легко оновлювати
- ✅ Не потребує прав адміністратора

### Для Production (Опціонально)

```bash
# Додати в PATH
echo 'export PATH="/Users/dev/Documents/GitHub/atlas/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# Тепер можна запустити звідкись
tetyana "завдання"
```

**Переваги:**
- ✅ Доступно з будь-якого місця
- ✅ Зручно для користувачів
- ✅ Все ще безпечно

### Для Системної Установки (НЕ рекомендовано)

```bash
# НЕ РОБИТИ!
sudo cp bin/tetyana /usr/local/bin/
```

**Проблеми:**
- ❌ Потребує sudo
- ❌ Складно оновлювати
- ❌ Залежності в проекті, а не в системі
- ❌ Не портативно

---

## 📊 Порівняння Варіантів

| Аспект | Поточна | PATH | Системна |
|--------|---------|------|----------|
| Запуск | `./bin/tetyana` | `tetyana` | `tetyana` |
| Права | Користувача | Користувача | Системні |
| Оновлення | Легко | Легко | Складно |
| Безпека | ✅ | ✅ | ❌ |
| Портативність | ✅ | ✅ | ❌ |
| Залежності | Проект | Проект | Система |

---

## 🎯 Висновок

### ✅ Поточна Архітектура

- **Бінарник:** Bash скрипт в `bin/tetyana`
- **Локація:** В репозиторії проекту
- **Права:** 755 (користувача)
- **Виконання:** З користувацькими правами
- **Шляхи:** Відносні до проекту
- **Безпека:** ✅ Безпечна
- **Портативність:** ✅ Портативна

### ✅ Як Це Працює

1. Скрипт знаходить себе
2. Визначає корінь проекту
3. Перевіряє залежності
4. Запускає Python агент
5. Повертає результат

### ✅ Переваги

- ✅ Не потребує установки
- ✅ Не потребує прав адміністратора
- ✅ Легко оновлювати
- ✅ Портативна
- ✅ Безпечна

---

**Бінарник працює як обгортка для Python агента! 🚀**
